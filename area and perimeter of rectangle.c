#include <stdio.h>
int main()
{
    int perimeter,area;
    int l,b;
    printf("\nenter the length");
    scanf("%d",&l);
    printf("\nenter the breadth");
    scanf("%d",&b);
    perimeter=2*(l+b);
    area=l*b;
    printf("\nenter the perimeter of rectangle%d",perimter);
    printf("\nenter the area of rectangle%d",area);
    return 0;

}
